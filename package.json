{
  "name": "live-event-rec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A verdict on which Bay Area shows are worth your money in the next 90 days.",
  "scripts": {
    "verify": "node scripts/verify.mjs",
    "build:data": "node scripts/build-data.mjs",
    "serve": "npx --yes serve public",
    "bundle": "node scripts/bundle.mjs",
    "deploy": "git subtree push --prefix public origin gh-pages",
    "dev": "node scripts/dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "homepage": "https://natelevietnam.github.io/live-event-rec/"
}
